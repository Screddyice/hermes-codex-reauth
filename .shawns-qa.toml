# Shawns QA Assist per-repo config.
#
# Without a gate here the agent reports merge_eligible=false and hands every PR
# to a human, because nothing can vouch for the change. scripts/verify.sh parses
# every tracked shell and Python file.

[verify]
test = "bash scripts/verify.sh"
